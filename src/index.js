// დავალება: ჩამოწერე ქვემოთ მოცემული ცვლადები და მიეცი მნიშვნელობები
// არ გამოიყენო ფუნქციები — მხოლოდ ცვლადები და სტრინგის ოპერაციები

// ნაბიჯი 1: შექმენი `firstName` — შენი სახელი (სტრინგი)
let firstName = "";

// ნაბიჯი 2: შექმენი `lastName` — შენი გვარი (სტრინგი)
let lastName = "";

// ნაბიჯი 3: შექმენი `fullName` — firstName + " " + lastName (სტრინგების შეერთება)
// მინიშნება: გამოიყენე `+` ოპერატორი და სივრცე მათ შორის
let fullName = "";

// ნაბიჯი 4: შექმენი `age` — შენი ასაკი (რიცხვი, 0-ზე მეტი)
let age = 0;

// ნაბიჯი 5: შექმენი `city` — შენი ქალაქი (სტრინგი)
let city = "";

// ნაბიჯი 6: შექმენი `output` ტექსტი ასეთი ფორმატით:
// "My name is [fullName]. I am [age] years old and I live in [city]."
// მინიშნება: სტრინგებსა და ცვლადებს შორის გამოიყენე `+` ოპერატორი
let output = "";

// Export for tests
export { firstName, lastName, fullName, age, city, output };
